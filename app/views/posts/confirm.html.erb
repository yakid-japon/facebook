<div class="container">
    <div class="row" style="height: 50px"></div>
		<div class="row profile">
			<div class="col-sm-3">
				
			</div>
			<div class="col-sm-6">
				<h3 class="text-center">CONFIRMATION DE LA PUBLICATION</h3><hr>
                <%= form_with(model: @post, local: true) do |form| %>
                    <div class="form-group">
                      <label for="exampleInputEmail1">Message</label>
                      <p class="text-justify">
                          <%= @post.content %>
                      </p>
                      <%= form.hidden_field :content %>
                    </div>
                    <div class="form-group">
                      <label for="exampleInputFile">Photo à publier</label>
                      <div>
                        <%= image_tag @post.image.url, width: 200 if @post.image && @post.image.url %>
                      </div>
                      <%= form.hidden_field :image_cache %>
                    </div>
                    <%= form.submit "Valider", class: "btn btn-primary" %>
                    <%= form.submit "Retour", class: "btn btn-default", name: "back" %>
                <% end %>
                <p style="height: 130px"></p>
			</div>
		</div>
		<div class="row" style="height: 100px"></div>
</div>
<%= render "partials/footer" %>